This is a test of a testing suite designed to test the functionality of Pykron.

You can run the tests from the main directory (so you do not need to install Pykron) using:

```
python3.8 -m unittest -v
```
